import React, { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function HomePage() {
  return (
    <SafeAreaView>
      {/*TODO: Implementar a página "Home" em "components/layout"*/}
    </SafeAreaView>
  );
}
